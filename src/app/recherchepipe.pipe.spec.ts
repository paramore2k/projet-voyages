import { RecherchepipePipe } from './recherchepipe.pipe';

describe('RecherchepipePipe', () => {
  it('create an instance', () => {
    const pipe = new RecherchepipePipe();
    expect(pipe).toBeTruthy();
  });
});
