import { ForfaitsUrbainPipe } from './forfaits-urbain.pipe';

describe('ForfaitsUrbainPipe', () => {
  it('create an instance', () => {
    const pipe = new ForfaitsUrbainPipe();
    expect(pipe).toBeTruthy();
  });
});
