import { ForfaitsVedettePipe } from './forfaits-vedette.pipe';

describe('ForfaitsVedettePipe', () => {
  it('create an instance', () => {
    const pipe = new ForfaitsVedettePipe();
    expect(pipe).toBeTruthy();
  });
});
